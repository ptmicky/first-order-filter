# 一阶低通滤波（指数移动平均滤波）MATLAB实现
基于公式 `y = k·y_prev + (1-k)·x` 的一阶低通滤波函数，适用于传感器数据平滑、信号去噪等场景。

## 功能特点
- 支持单值/数组输入，适配批量数据处理
- 完善的输入校验，避免非法参数导致的错误
- 保留滤波历史值，实现连续迭代滤波
- 滤波系数k自动边界限定，提升鲁棒性

## 使用方法
### 1. 单值滤波
```matlab
last_y = 20; % 初始化历史值
[y, last_y] = firstOrderLowpassFilter(25, 0.8, last_y);
